import { ComingSoon } from "../components/studio/ComingSoon";

export default function StudioQuizzes() {
  return <ComingSoon title="Quizzes" blurb="Your full quiz library lands here — for now the gallery is on Home." />;
}
