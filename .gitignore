node_modules
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*
# …but the placeholder template IS tracked (docs for every env var; no real values)
!.env.example

yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Claude Code session state
.claude/

# Playwright e2e smoke-test artifacts (screenshots + reports). NOTE: the
# committed screenshot BASELINES live in /e2e/*-snapshots/ and are tracked.
/e2e/shots/
/test-results/
/playwright-report/
scripts/audit/
wf-audit-*.mjs
wf-*.png
wf-*.mjs
